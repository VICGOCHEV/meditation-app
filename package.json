{
  "name": "meditation-app-monorepo",
  "private": true,
  "description": "Корень монорепо. 3 поддиректории-приложения: application/ (Vite-фронт), backend/ (Fastify), cms/ (кастомная CMS).",
  "scripts": {
    "dev": "cd application && npm run dev",
    "build": "cd application && npm run build",
    "preview": "cd application && npm run preview",
    "dev:cms": "cd cms && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "lint": "cd application && npm run lint"
  }
}
