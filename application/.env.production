# Production build values — picked up automatically by `npm run build`.
# Overrides `.env` (которое предназначено для локальной разработки с моком).
VITE_API_URL=/api
VITE_USE_MOCK=false
VITE_USE_CMS=true
VITE_CMS_URL=/api/content
VITE_COMPANIONS_MIN=47
VITE_COMPANIONS_MAX=740
